# take command line arguement


# switch case for command line arguement
    # "run" docker compose normally

    # "run_and_update" docker compose and update

    # "run_clean_slate" docker compose on a clean slate (purge all containers, images, and cache but not volumes)

    # "pull_updates" git pull using token

    # "logs" show docker logs

    # "shell" shell into container

    # "help" list the commands and descriptions



# optional later on stuff
    # copy_to_remote shortcut use on local system to copy helper.sh to remote system

    # "alias" add "helper" alias to bashrc

    # "first_run" perform checks, make sure dir for bind mount exists, check folder/file perms, etc